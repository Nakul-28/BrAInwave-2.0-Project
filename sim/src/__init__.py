"""Top-level package marker for the source package.

This file makes ``src`` a Python package so tests can import modules like
``src.main`` and ``src.envs.*``. It intentionally contains no runtime logic.
"""
